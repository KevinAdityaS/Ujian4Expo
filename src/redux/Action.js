export function ReportAction(inputType, inputValue){

    return {type : "SET_REPORT", inputType : inputType, inputValue : inputValue}

}

export function UserAction(inputType, inputValue){

    return {type : "SET_USER", inputType : inputType, inputValue : inputValue}

}